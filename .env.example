# Copy to .env and replace the blanks. Do not deploy these placeholders.
# node -e "console.log(require('crypto').randomBytes(32).toString('base64url'))"
DATABASE_URL="postgresql://postgres:postgres@localhost:5433/security_plan?schema=public"
AUTH_SECRET=""
AUTH_URL="http://localhost:3000"
CVE_SYNC_SECRET=""
# Demo accounts and the login hint stay off unless this is 1.
DEMO_LOGIN="0"
